[pytest]
addopts = -q --cov=. --cov-branch --cov-report=term-missing:skip-covered --cov-fail-under=85
testpaths = tests
markers =
    integration: integration tests hitting Flask routes
    perf: performance tests (non-blocking in CI)
